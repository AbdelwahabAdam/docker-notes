version: "3"
services:
  node-app:
    build:
      context: .
      # args:
      #   - NODE_ENV=production
      target: production

    environment:
      - NODE_ENV=PRODUCTION
    command: npm start